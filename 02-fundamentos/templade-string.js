const nombre = "Deadpool";
const real = "Wade Winston";

const normal = nombre + " " + real; // Forma uno
const templade = `${nombre} ${real}`; // Con templade o backticks

console.log(normal);
console.log(templade);

const html = `
<h1>Hola</h1>
<p>Mundo</p>`;

console.log(html);
